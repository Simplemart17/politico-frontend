import React from 'react';
import { RingLoader } from 'react-spinners';

const ContentLoader = () => (
  <RingLoader
    sizeUnit={'px'}
    size={150}
    color={'#4d7b51'}
  />
);

export default ContentLoader;
